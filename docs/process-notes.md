# Process Notes

## 2026-07-16 — Ran /cleanup on a 99%-full drive; found 2 safety bugs + 3-way copy drift
- **Done:** Real run (0.7 GB free). Freed ~8.5 GB (3.7 → 12.2 measured pre-deletion → after). Patched two safety bugs into cleanup.md; filed #6–#12; merged three drifted copies to byte-identical at 1069 lines.
- **Decisions:** Canonical = claude-config's copy (it was ahead) + the `_npx`/`%TEMP%\claude` fixes → propagated to `claude-cleanup` and `~/.claude/commands/`. Kept `npm cache clean --force` over the "fast" `rmdir` path.
- **State:** **The two safety bugs both nearly fired.** (1) The command named `npm-cache` as a `scrub.ps1` target and as the `rmdir /s /q` example — but `%LOCALAPPDATA%\npm-cache\_npx\` is where `npx -y <pkg>` materialises packages, so live MCP servers run from inside it (harmonica-mcp, context7, shadcn — one set per session). `scrub.ps1` returned "Access is denied" *because* the servers held it; the lock was the only thing that prevented it. (2) `%TEMP%\claude` (Claude Code's own scratch) was excluded only as `claude-cleanup`, got swept in, and killed a Bash call mid-flight (ENOENT). Also survived only by being locked.
- **Worse — the copy that loads was the stale one.** `~/.claude/commands/cleanup.md` (1026 lines) was missing the live-MCP-server `node_modules` rule this repo added 2026-06-29 after two book-power MCPs broke with `-32000`. Running it duly offered `power-of-facilitation` + `facilitating-deliberation` node_modules (both back registered MCP servers with live processes, both flagged INACTIVE by git). Only the user not selecting that category prevented a repeat. A safety rule in a file nobody loads is not a safety rule.
- **Next:** #12 (copy drift — root cause) and #7 (parallel-session detection) are the two that address the class rather than the instances. Four of five near-misses today were caught by a file lock, not by the tool.
